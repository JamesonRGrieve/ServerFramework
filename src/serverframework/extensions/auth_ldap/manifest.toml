name = "auth_ldap"
version = "1.0.0"
description = "LDAP/Active Directory authentication (consumer: bind against external directory; provider: act as LDAP server)"

[[extension_dependencies]]
name = "auth_session"
optional = false

[python_dependencies]
ldap3 = ">=2.9.1"
